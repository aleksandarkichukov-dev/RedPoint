{
  "name": "@redpoint/catalog",
  "version": "0.0.0",
  "private": true,
  "//": "CommonJS on purpose. The Medusa backend is CJS and cannot require an ESM package; the scraper is ESM and imports CJS through normal interop. One format that both consumers can read beats a dual build for a package this small.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node scripts/check-bulk.js"
  },
  "dependencies": {
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
